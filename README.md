# Vectorless-RAG-

Vectorless Rag is retrieval without using database, chuncking, embedding models, technique, it simply uses techniques of tree structure with edges and nodes. It is heavily rely in LLM as for LLM for tree builder and LLM for Tree Search. 